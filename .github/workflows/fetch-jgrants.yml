name: Fetch jGrants

on:
  workflow_dispatch: {}   # 手動実行
  # 毎日自動実行したいときは以下を有効化（UTC21:00=JST朝6時）
  # schedule:
  #   - cron: "0 21 * * *"

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pyyaml pytest

      - name: Run smoke tests
        run: |
          pytest -q

      - name: Run jGrants fetch (small run)
        run: |
          mkdir -p data
          python etl/fetch_jgrants.py --out data/jgrants.jsonl \
            --max-keywords 20 --max-details 300 --max-pages 30 --page-size 20 --sleep 0.5

      - name: Upload jGrants artifact
        uses: actions/upload-artifact@v4
        with:
          name: jgrants-jsonl
          path: data/jgrants.jsonl
          if-no-files-found: error
          retention-days: 7
